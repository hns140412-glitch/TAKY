# TAKY GUIDE / FAMILY LEARNING OS

Status: REV_00 / PRE-CONFIRMATION EVOLVING DESIGN SOURCE
Role: Canonical lower-layer OS master for GUIDE / Family Learning OS under TAKY GRAND MASTER.
Authority: TAKY / GRAND MASTER > GUIDE FAMILY LEARNING OS > SUBJECT / PRACTICE SUB-MASTER + FUNCTIONAL APP > SKILL > TOOL / AGENT.

This file recovers and normalizes materially confirmed GUIDE rules from recoverable prior GUIDE MASTER lineage, Handoffs, attached/recoverable source documents and later user corrections. Historical labels such as Guide REV_04 / REV_05 / Ready & Set REV_06 are lineage only; current official state remains REV_00 until explicit user finalization.

## 0. MASTER DECLARATION — HARD LOCK

Primary purpose:
The child repeatedly learns to understand assigned work, set goals, allocate work within real life time, start at an appropriate time, finish, reflect and improve the next plan, so that self-directed learning management and time awareness grow over time.

Secondary purpose:
Observe actual learning experience — time, amount, difficulty, success, mistakes, stuck points, explanations, help/check events and performance evidence — according to subject characteristics, so the child and parent can make the next learning cycle better.

PRIORITY SHALL NOT BE REVERSED.

`계획 → 실천 → 경험 → 관찰 → 개선 → 더 나은 다음 계획`

Learning analysis is not a higher-order justification for increasing study volume.
Guide success = the child needs Guide less over time.

## 0.1 Work OS / Learning OS sibling boundary — HARD LOCK

Family Learning OS is a first-class OS under TAKY, not a child module of Work OS.

Current relation:
`TAKY -> WORK_OS`
`TAKY -> LEARNING_OS`

Shared technical services may be reused only when they remain semantic-light. Child/parent/family identity, relationships, learning roles/permissions, child-data authority and learning-plan/history meaning remain Learning-owned and SHALL NOT be inherited from Work organization/admin authority.

If the same human participates in both domains, account linkage does not create cross-domain authority. Any future linkage requires an explicit scoped federation/projection contract.

Machine-readable support map:
`MASTER/SYSTEM_LAYER_OWNERSHIP_MAP.json`.

## 1. OWNERSHIP / AUTHORITY — HARD LOCK

`MAIN = HOW TO MANAGE`
`SUB-MASTER = HOW TO UNDERSTAND`
`FUNCTIONAL APP = HOW TO HELP EXECUTE`

MAIN owns:
- life schedule / available-time interpretation at management level
- weekly/daily allocation
- priority and deadline coordination
- workload placement using subject interpretation
- Carry-over and rescheduling
- plan-vs-actual adjustment
- next-plan forecast

SUBJECT / PRACTICE SUB-MASTER owns:
- subject/practice meaning
- difficulty and cognitive-load interpretation
- concept / skill target
- divisible learning-unit recommendation
- method recommendation
- subject-specific performance/observation interpretation
- uncertainty / confidence

FUNCTIONAL APP owns:
- the actual execution experience: start, focus, return, complete, learn, write, practice, record, save, share and related interaction according to its project contract.

Hard boundaries:
- Subject Sub-Master SHALL NOT own final weekly/daily whole-life allocation.
- MAIN SHALL NOT invent subject difficulty without subject/evidence support.
- Functional App SHALL NOT absorb MAIN schedule/family/common authority.
- Guide SHALL NOT replace or evaluate the professional teacher role.
- External AI / Tool / Skill SHALL NOT override MASTER authority.
- `SUBJECT INTERPRETS; MAIN ALLOCATES.`

## 2. SCHEDULE FIRST, ASSIGNMENT SECOND — TOP OPERATING PRIORITY

`SCHEDULE FIRST, ASSIGNMENT SECOND`

The planning engine begins from the child's real life schedule and available windows, not from a pile of homework.

Canonical management flow:
`CHILD LIFE SCHEDULE → WEEKLY AVAILABLE WINDOWS → ASSIGNMENT INTAKE → SUBJECT INTERPRETATION → LEARNING UNIT SIZING → WORKLOAD / DIFFICULTY → CAPACITY / DEADLINE → WEEKLY PLAN → DAILY ADAPTIVE TODO → CHILD CHOICE → EXECUTION → REAL-TIME ADJUSTMENT → RESULT / CHECK / HELP → SUBJECT OBSERVATION → MAIN SUMMARY → PARENT SUPPORT → CALENDAR / HISTORY → FORECAST → NEXT PLAN`

Closed-loop realization flow:
`CHILD LIFE SCHEDULE → ASSIGNMENT INTAKE → MAIN PLAN → PLAN CONFIRM → EXECUTION → SUB-MASTER / APP → ARTIFACT → REVIEW / FINALIZE → LOCAL SAVE → SHARE / SUBMISSION → CALENDAR → HISTORY → MAIN OBSERVATION → NEXT PLAN`

Local result save precedes reliance on external share/calendar acknowledgement. External failure SHALL NOT delete or invalidate the child's locally committed learning result.

## 3. SCHEDULE / CAPACITY CONTRACT — HARD LOCK

Default time unit may use `25분 학습 + 5분 휴식`, but this is a planning aid, not a mechanical mandate.

Morning:
- treat before-school time as a compressed session;
- initial 1-block level may expand to two short focus blocks as adaptation grows;
- prefer an overall approximately 40-minute morning target over mechanically forcing 55 minutes.

After academy / main study:
- short available windows may be used when appropriate;
- 3–4 blocks after dinner may represent capacity, not mandatory volume.

Hard locks:
`FREE TIME EXISTS ≠ MUST STUDY`
`CAPACITY ≠ REQUIRED STUDY AMOUNT`
`PLANNED STUDY END ≤ 22:00`
`22:00 PLANNED END ≠ FORCED ACTUAL SHUTDOWN`
`OVERTIME ≠ FOCUS FAILURE`
`OVERTIME ≠ STUCK`

If actual execution passes the planned boundary, distinguish possible reasons such as start delay, focus variation, difficulty, estimate error, break overrun, productive deep thinking or actual stuck state instead of automatically assigning failure.

Plan contract candidate retained for continued validation:
`PLAN IS NEGOTIABLE; DEADLINES / EXTERNAL COMMITMENTS ARE CONSTRAINTS; NOT EVERY PLANNED TASK IS MANDATORY TODAY.`

This remains CANDIDATE until explicitly finalized, but current behavior SHALL NOT contradict the confirmed hard locks above.

## 4. WORKLOAD / ALLOCATION CONTRACT

MAIN allocates learning load, not empty time.

Primary allocation inputs:
`available time + deadline + expected duration + difficulty / cognitive load + divisibility + recent actual performance + time-of-day characteristics`

Difficulty / load interpretation may include:
- volume / repetition
- new concept
- reasoning
- reading
- writing
- calculation
- familiarity
- likely parent/teacher involvement

Fixed rules such as “hard work always goes in the morning” are prohibited unless supported by actual child pattern/evidence.

## 5. LEARNING UNIT SIZING

Physical assignment amount ≠ executable learning unit.

`ASSIGNMENT → SUBJECT INTERPRETATION → CONCEPT / SKILL TARGET → DIVISIBLE LEARNING UNITS → DIFFICULTY / COGNITIVE LOAD → ESTIMATED EFFORT RANGE`

Use the subject-appropriate unit: page, problem, section, chapter, passage, word set, performance segment, piano section, etc.

Subject interprets units; MAIN combines those units with schedule, capacity and deadline.

## 6. TASK STATE / CARRY-OVER / TIME AWARENESS

Canonical task states:
`PLANNED → READY → IN_PROGRESS → COMPLETED`
or
`DEFERRED → CARRY_OVER → RESCHEDULED`

Incomplete work is not deleted or punished.
`미완료 = 다음 계획에서 내가 해결해야 할 Carry-over`

Useful plan-vs-actual evidence includes:
- plannedStartAt
- actualStartAt
- startDelay
- plannedDuration
- actualDuration

Repeated plan-vs-actual differences improve future estimates and allocation; they do not automatically become diagnosis or blame.

## 7. CHILD OWNERSHIP — HARD LOCK

`Guide 주도 보조 → 선택권 이전 → 아이 계획 → 아이 조정 → 아이 주도 / Guide 대기`

A single failure or late day SHALL NOT cause Guide to reclaim full control.
MAIN recommendations are recommendations, not commands.
Ready & Set must allow the child to agree, adjust and execute the current goal within the applicable plan/constraint boundaries.

## 8. GUIDE CORE / INTERVENTION — HARD LOCK

Guide is not the child's answer proxy, grader, surveillance system or permanent controller.

Base behavior:
`관찰 → 필요한 순간만 개입 → 생각할 여지 → 쉬운 도움 → 아이에게 다시 주도권 반환`

- Silence by Default while the child is working well.
- compare with the child's past self, not others.
- avoid blame and inflated praise.
- use age-adaptive easy language first.
- easy words → terminology only when useful.
- short, clear, actionable first; depth when wanted/needed.

Hard locks:
`OBSERVATION ≠ DIAGNOSIS`
`LONG TIME ≠ STUCK`
`CHILD SELF-REPORT ≠ VERIFIED LEARNING OBSERVATION`
`OVERTIME ≠ FOCUS FAILURE`
`CHECK_REQUEST ≠ HELP_REQUEST`
`QUESTION SUBJECT ≠ CURRENT TASK SUBJECT`

## 9. WHY / CURIOSITY / LEARNING MODE

Self-direction includes discovering WHY, exploring, connecting prior knowledge and explaining in the child's own words.

`Guide WHY → child notices WHY → child creates WHY → explores / connects / judges independently`

WHY is not a technique for refusing direct answers.
Question count SHALL NOT become a score/reward competition.
Question/help ≠ weakness.

Focused Practice:
`FOCUS > EXPLORE`
- short question: quick answer then return;
- deeper question: save to Curiosity Record and return, then explore later.

Deep Thinking:
`WHY → explore → connect → understand → explain in own words → possible new WHY → return`

Final learning mode uses:
`MAIN operating context + SUBJECT interpretation + current child state → FINAL SESSION MODE`

## 10. ANSWER FIRST / QUESTION INTENT — HARD LOCK

1. CURIOSITY / KNOWLEDGE QUESTION
→ direct easy answer → easy reason → optional example/connection.

2. HOMEWORK FINAL ANSWER REQUEST
→ concept/method/hint → do not write the final homework answer on the child's behalf.

3. RETRIEVAL / PRACTICE QUIZ
→ child recalls/answers first → then feedback/explanation.

`ANSWER FIRST ≠ ALWAYS SHOW ANSWER`
`OFF-TOPIC ≠ DISTRACTION`

## 11. LEARNING METHOD ROUTER

Reusable method candidates:
`UNDERSTAND / RETRIEVE / APPLY / COMPARE / EXPLAIN / VISUALIZE / PRACTICE / CREATE / PERFORM / REFLECT`

Method ≠ subject. The same method is interpreted differently by each subject.
Do not force one method across all subjects.
Retrieval does not replace new-concept understanding.
Speed recall does not dominate thinking/creative tasks.
Piano/practice requires actual performance evidence where applicable.

## 12. ASSIGNMENT INTAKE / OCR / ANSWER KEY

Parent homework intake SHALL prioritize uninterrupted capture. The parent does not classify each photo while shooting, and the capture surface does not emphasize a running photo count.

Canonical flow:
`ASSIGNMENT INTAKE → CONTINUOUS CAPTURE → AUTO LOCAL TEMP SAVE → IMMEDIATE NEXT CAPTURE → ANALYSIS HANDOFF → BATCH OCR → GROUP / CLASSIFY → QUALITY GATE → TARGETED RETAKE WHEN NEEDED → TARGETED RE-ANALYSIS → REVIEW-BEFORE-COMMIT → CONFIRMED ASSIGNMENT → GOOGLE DRIVE SAVE / SYNC → RETURN TO CAPTURE`

The analysis handoff is a batch boundary, not the end of capture.
`ANALYZE NOW ≠ CAPTURE COMPLETE`
After analysis, the parent may immediately continue capturing another batch.

Capture interaction contract:
- The normal camera surface prioritizes the live view and bottom shutter.
- Each shutter action immediately creates a durable local temporary original and returns to the next shot without a blocking modal, classification step or verbose toast.
- Add / delete / retake remain available outside the rapid shutter path.
- Photo count may exist as internal state or recovery evidence, but SHALL NOT compete with the shutter during normal capture.
- Each original keeps an immutable `captureId`, `batchId` and stable `displayOrder`. The visible sequence number is derived from that order.
- A quality failure SHALL identify the exact photo by thumbnail + sequence and request only that photo again.
- Retake replaces the targeted original, preserves valid photos/results, prevents duplicate membership and re-runs only the affected analysis where safe.
- One bad photo SHALL NOT force a full-batch restart.

Grouping evidence may include cover, subject/unit/title, problem content, page number, capture order, layout similarity and answer-key pattern. Capture order is evidence, not an absolute rule.

`CAMERA ORIGINAL ≠ OCR DRAFT ≠ CONFIRMED ASSIGNMENT`
`LOCAL TEMP SAVE ≠ GOOGLE DRIVE SAVE ACK`
`BATCH ANALYSIS PASS ≠ HUMAN REVIEW / COMMIT`

System/action copy is concise and literal:
- primary analysis action: `분석 맡기기`
- OCR: `사진 분석 중`
- classification: `자료 분류 중`
- review pass: `결과 확인 중`
- batch result: `분석 완료`
- repair action: `N번째 장 다시 찍기`
- external save acknowledgement: `Google Drive 저장 완료`
- next action: `이어서 촬영하기`

Guide copy carries warmth and wit without obscuring the action:
- analysis handoff: `좋아, 이제 내 차례네.`
- OCR: `사진 속 단서부터 꺼내는 중.`
- classification: `비슷한 건 모으고, 수상한 건 따로.`
- review pass: `놓친 게 있나 한 바퀴만 더.`
- success / return: `정리 끝. 다음 사진도 가져와.`
- glare retake: `N번째 장은 빛이 먼저 나섰네. 이 장만 다시 부탁해.`
- blur retake: `N번째 장은 초점이 살짝 길을 잃었네. 이 장만 다시 부탁해.`
- crop retake: `N번째 장은 아래쪽이 숨었네. 이 장만 다시 부탁해.`
- duplicate check: `N번째 장은 앞 사진과 너무 닮았는데? 한 번만 확인해줘.`
- Drive acknowledgement: `제자리까지 잘 데려다 놨어.`

Copy hard locks:
- `시스템은 정확하게, Guide는 따뜻하고 위트 있게.`
- Guide does not joke about the child, the parent's mistake or learning weakness. Wit may target light, focus, the page, the camera/system or Guide itself.
- Keep lines short, calm and non-babyish. No inflated praise, diagnosis, blame or technical explanation.
- Per-shot Guide speech SHALL NOT interrupt rapid capture.
- `촬영 완료`, persistent count emphasis, per-shot forced classification, generic sound-effect copy without state meaning and full-batch restart for one bad image are SUPERSEDED / REJECTED.

Ownership boundary:
- GUIDE / MAIN owns assignment-intake interpretation, grouping, review and confirmation.
- Assignment Camera/OCR intake is a shared GUIDE capability. A functional app may render or call it but SHALL NOT redefine its meaning or absorb MAIN authority.
- Provider choice, exact OCR confidence thresholds, retry limits and live background analysis scheduling remain HOLD until project/runtime validation.
- PWA implementation, deployment and real-device evidence are outside this logic/copy delta and remain UNVERIFIED.

### 12.1 ASSIGNMENT CYCLE / SOURCE REGISTRY — RECOVERED HARD CONTINUITY CONTRACT

A recoverable assignment cycle SHALL relate the source handed out, answer/SOL material when legitimately available, completed work, and capture/analysis evidence without relying on folder-name guesswork.

Recovered logical registry:
`CHILD PROFILE → assignmentRoot → ASSIGNMENT CYCLE`

An Assignment Cycle may track:
- `cycleId`
- `childId / memberId`
- `assignmentRoot`
- `distributedSourceRefs[]` — 숙제배포 / original distributed-source references
- `solRefs[]` — SOL / answer-solution references with access boundary preserved
- `completedRefs[]` — 숙제완료 / completed-work references
- `captureSessionIds[]`
- `assignmentStatus`
- `reviewStatus`
- `retentionState`

Hard distinctions:
- `FOLDER NAME ≠ RELATIONSHIP PROOF`
- `DISTRIBUTED SOURCE ≠ SOL ≠ COMPLETED WORK`
- `ASSIGNMENT CYCLE ≠ CAPTURE BATCH`
- `ANALYSIS BATCH ≠ ASSIGNMENT CYCLE END`
- `SOL EXISTS ≠ CHILD MAY ACCESS SOL`

The logical relationship is required before automating cross-file recovery, comparison, grading/check or cleanup. Exact Drive folder naming/storage adapter remains implementation-owned and must preserve the logical cycle contract.

Deletion / weekly cleanup is NOT automatically authorized by this registry. Retention period, immutable originals, long-term learning-history separation and rollback/recovery remain explicit policy gates. Until those are confirmed, destructive cleanup stays `HOLD`.

Answer-key source is separate from assignment source.
`ANSWER KEY EXISTS ≠ CHILD CAN ACCESS ANSWER`

Answer key may be registered before or after the child requests checking.
`ANSWER_KEY_REQUIRED` is not child failure.
AI grading is candidate assistance; uncertain handwriting/interpretation requires human review. AI forced correction/relearning remains HOLD.

## 13. HELP / CHECK / FAMILY / TEACHER BOUNDARY

CHECK:
`SOLVING → SOLVED → CHECK_REQUESTED → CHECKED`
CHECK = child has finished and asks for parent confirmation/grading.

HELP:
`SOLVING → STUCK_POSSIBLE → STUCK_SPECIFIC / UNCLEAR → UNKNOWN_STUCK → HELP_REQUESTED → HELP_RECEIVED / RETURN`
HELP = human help needed during learning.

`CHECK_REQUEST ≠ HELP_REQUEST`
CHECK-specific audio SHALL NOT be reused as HELP semantics.

Stuck detection:
`STUCK DETECTION ≠ STUCK JUDGMENT`
`오래 걸림 ≠ 막힘`
Ask a short confirmation first; if the child says they are thinking, retreat. Do not infer the cause and launch a long explanation without evidence.

Guide / Parent / Teacher:
- Guide: observe, connect, explain briefly, hint, identify patterns, route help.
- Parent: judge real-world need, correct AI interpretation, respond to HELP/CHECK, support schedule.
- Teacher: professional teaching, misconception correction, additional-practice range, repertoire/professional-performance judgment.

Parent Mode is not surveillance.
Parent support states may include:
`PROGRESSING / CHECK_NEEDED / HELP_NEEDED / SCHEDULE_ADJUSTMENT / ENCOURAGEMENT_MOMENT`
Use event-driven support, not constant monitoring.

Family routing:
`current subject → HELP/CHECK → default helper → current day/time → family availability → child's explicit choice → Guide TTS → connection`
`LIVE FAMILY STATE > DEFAULT FAMILY SCHEDULE`

## 14. VOICE / AUDIO / PRACTICE EVIDENCE

Conversation voice session:
`ENTER → MIC PERMISSION → AUDIO HANDOFF → GUIDE SPEAKING → LISTENING → CHILD SPEAKING → END-OF-SPEECH → STT / intent → SUB-MASTER → GUIDE RESPONSE → ... → EXIT → MIC OFF → restore original timer/BGM/task`

Hard behaviors:
- Timer keeps running unless the child explicitly pauses under the app contract.
- BGM pauses/ducks when it would compete with voice/recording.
- Mic is active only during an active session.
- Raw audio permanent save OFF by default.
- `듣는 중 ≠ 녹음 저장 중`.
- TTS provider is replaceable.

Voice Practice Engine may support:
`Read Aloud / Listen Back / Guide Model Reading / Expression Compare / Retry / Final Take / Audio File / Share-Submit`

Recording infrastructure is shared-capability; subject interpretation is domain-owned.

Practice Evidence candidate flow:
`BEFORE → PRACTICE → AFTER → COMPARE → PRAISE → ONE NEXT IMPROVEMENT`

Hard separation:
`RECORDING ≠ FINALIZED ≠ SHARED ≠ SUBMITTED`
`STOPPED ≠ FINALIZED`
`FINALIZED ≠ SHARED`
`SAVED ≠ SHARED`
`SHARE BUTTON ≠ SHARED`
`SHARED ≠ SUBMITTED`

## 15. FAMILY PLATFORM / DATA / STORAGE / SYNC

Family platform candidates/capabilities include authentication, member identity, role/permission, profile, storage connection, sync, AI credential gateway, audio/camera permission, calendar, HELP/CHECK transport, shared settings and learning-artifact submission.

Identity separation:
- authentication ≠ member identity ≠ role ≠ storage connection.
- memberId is the internal stable identity; visible names/kinship labels are presentation/context.
- do not require a separate Google account for each child.

Storage:
`LOCAL-FIRST + DRIVE-BACKED HYBRID`
- Device Local DB = execution state/cache/durable local state as required by project contract.
- Google Drive = long-term/family storage and synchronization surface.
- Sync Engine = version/conflict/reconciliation.

Important sync events may include assignment, plan confirm, completion, CHECK, HELP, answer key and important settings. Checkpoint/night sync may handle ordinary data, but sync SHALL NOT interrupt active late learning merely because a clock threshold is reached.

Multi-volume storage may rotate when capacity requires:
`OLD STORAGE = READABLE`
`NEW STORAGE = WRITABLE`
Forced migration of all historical files is not required unless a later approved policy requires it.

Family privacy:
`FAMILY BOUNDARY = DATA BOUNDARY = STORAGE BOUNDARY`
`SHARED ENGINE ≠ SHARED DATA`
GitHub is not the private child-learning-data store.

## 16. OFFLINE / CONTINUITY — HARD LOCK WHEN APPLICABLE

For Ready & Set / GUIDE runtime where offline capability is required:
`INTERNET LOST ≠ TIMER STOP`
`APP BACKGROUND ≠ TIMER STOP`
`SCREEN OFF ≠ TIMER STOP`
`LOCAL STATE + TIMESTAMP = TIMER SOURCE OF TRUTH` for the project runtime contract.

`LOCAL COMMIT → SYNC_PENDING → restore/retry → validate → SYNCED`
`AI UNAVAILABLE ≠ GUIDE UNAVAILABLE`
`SYNC FAILURE ≠ LOCAL DATA LOSS`

These semantics inherit GRAND MASTER connected/offline runtime governance. Actual runtime/offline convergence remains UNVERIFIED until tested with representative implementation evidence.

## 17. READY & SET — FUNCTIONAL APP REGRESSION CONTRACT

Ready & Set is an execution layer, not the owner of whole-homework analysis or the whole weekly schedule.

`MAIN = 무엇을 / 언제 / 얼마나 하면 좋을지 계획한다.`
`READY & SET = 지금 무엇을 할지 아이와 합의하고 실행한다.`

Core purpose:
`스스로 시작 → 실제 집중 → 끝까지 완료 → 흔들려도 복귀 → 시간감각 → 성장 발견 → 가족 공유`

Time Attack = Core Focus Experience, not the standalone product identity.

Main surfaces may include TIMER / TODAY / WEEK, but exact UI behavior remains PROJECT-owned and must preserve project-specific approved states.
Timer/BGM/Audio/Guide engines may be shared without erasing Ready & Set identity.

Project-specific detailed UI, swipe, timer, lock-screen, orientation and visual rules remain in Ready & Set PROJECT master / lineage until migrated and validated into TAKY canonical project ownership.

## 18. OTHER FUNCTIONAL APP REGRESSION CONTRACTS

Hide & Seek:
`사진 / Camera intake → OCR Review-before-Commit → 단어/의미 학습 → 취약단어 → CODE RED → RETRACE → 완료`
Preserve cumulative word/sense memory, camera-first intake, OCR Review-before-Commit, weak-word priority, CODE RED and retrace identity.
The former product name and police/capture child-facing world are SUPERSEDED; validated learning-core lineage remains protected through the current Hide & Seek owner.

Snap & Pop:
Preserve thinking/writing/creation purpose. Guide does not become the writer/grader. Five writing tools remain freely usable under its current project decisions; old map/tool unlocking is superseded. Family Expansion remains separated from core unless explicitly integrated by project authority.

Piano Practice:
Maintain separate Practice Sub-Master + App. AI is a practice coach, not teacher replacement; compare primarily against the child's own prior performance and use actual practice/performance evidence.

## 19. SHARED LEARNING CAPABILITIES / LEARNING ENGINE BOUNDARY

The Guide-facing shared capabilities in this section are NOT the Learning Engine Core.

Learning Engine Core authority is defined in `OS/LEARNING_ENGINE_CORE.md` and owns learner-state interpretation, evidence interpretation, uncertainty and pedagogical intent.

Guide-owned shared capabilities may include:
- Focused Practice / Deep Thinking modes
- Learning Method Router
- Curiosity Engine
- Quick Ask
- Imagination Cloud
- Stuck Detection
- HELP/CHECK transport
- Reflection-Return
- Voice Practice Engine
- Practice Evidence Engine
- Visual Writing Coach Engine candidate

Meta-regulation candidate:
`PLAN → DO → MONITOR → ADJUST → EVALUATE`

Boundary:
- `SUBJECT / PRACTICE SUB-MASTER = subject/practice interpretation`
- `LEARNING ENGINE CORE = learner-state + evidence interpretation + pedagogical adaptation`
- `MAIN / PLANNER = whole-life allocation + dated placement`
- `FUNCTIONAL APP = execution experience`
- `GUIDE CAPABILITY = child-facing support / intervention`

Shared capability ownership SHALL NOT merge private data or erase domain interpretation ownership.
Learning Engine Core SHALL NOT absorb Ready session runtime, Planner date authority or specialist-app UI.

## 20. DATA & PRIVACY — HARD LOCK

Keep distinct as applicable:
`CAMERA ORIGINAL / OCR DRAFT / CONFIRMED ASSIGNMENT / ANSWER KEY SOURCE / LIVE AUDIO BUFFER / SAVED RECORDING / TRANSCRIPT / CHILD SELF-REPORT / SUBJECT OBSERVATION / MAIN ACTIVITY SUMMARY / CURIOSITY RECORD / CHECK EVENT / HELP EVENT / LEARNING ARTIFACT`

Rules:
- child-specific/family-specific isolation
- minimum retention / minimum transfer
- raw audio permanent save OFF by default
- answer-key access separated from child access
- only minimum necessary subject data returns to MAIN
- self-report, observation, HELP, CHECK and curiosity/weakness semantics remain distinct.

## 21. LEARNING ARTIFACT / SUBMISSION

Artifact fields may include:
`artifactId, memberId, taskId, subject, activityType, originalFile, derivedFile?, recordedAt, finalSelectedAt, dueAt?, reviewStatus, finalizationStatus, shareStatus, submissionStatus, calendarStatus, historyLink`

Hard separation:
`RECORDING ≠ FINALIZED ≠ SHARED ≠ SUBMITTED`
Share sheet open ≠ submission complete.

## 22. CROSS-APP / EXTERNAL TOOL GOVERNANCE

Before moving/merging a function:
`new/changed feature → existing owner? → other apps need it? → duplicate? → shared engine candidate? → app-specific? → MAIN-owned? → call-only? → regression risk? → KEEP / SHARE / MOVE / MERGE / CALL / REJECT`

Typical shared candidates: Timer Engine, Audio Handoff, Guide TTS, Quick Ask, HELP/CHECK transport, Assignment Camera/OCR intake.

Typical KEEP examples: Hide & Seek cumulative word memory/CODE RED, Snap & Pop ideation, Piano performance interpretation, MAIN schedule/load allocation.

External learning/AI tools are reference/capability candidates, not authority.
`NEED → SKILL / METHOD → TOOL`
Comparative learning patterns may be extracted, but external product structure is not copied wholesale.

## 23. AI PROVIDER / CREDENTIAL

`Guide Logic → AI Router → Provider Adapter`
Provider selection uses task fit, accuracy, multimodal ability, cost, privacy and availability. Model names/prices/free limits are not hard-coded into MASTER governance.

One provider limit/failure ≠ whole GUIDE failure.
Automatic paid overage requires explicit user/family policy.

Secret API keys:
- not in GitHub source
- not in PWA JS
- not plaintext LocalStorage
- not child UI

Preferred security shape where applicable:
`Child PWA → AI Gateway → Family Credential → Provider`

## 24. STATUS / DISPOSITION RECOVERY

### PRESERVE / ACTIVE
- primary > secondary purpose
- child ownership transfer
- MAIN / SUB-MASTER / FUNCTIONAL APP authority split
- Subject interprets; MAIN allocates
- SCHEDULE FIRST, ASSIGNMENT SECOND
- schedule/capacity hard locks
- 22:00 planned-end semantics
- workload = learning-load allocation
- Carry-over/time-awareness
- WHY/self-directed learning
- Silence by Default
- Answer First question-intent distinction
- Focused Practice / Deep Thinking
- Quick Ask / return
- Stuck detection ≠ judgment
- HELP ≠ CHECK
- Guide / Family / Teacher boundary
- Voice/Timer/BGM separation
- Three-Dimension Growth separation
- Assignment continuous multi-photo intake + Review-before-Commit
- Assignment Cycle / distributed-source / SOL / completed-work relationship registry
- Answer-key timing/access separation
- Learning Unit Sizing
- Learning Method Router
- Family identity/auth/storage separation
- Local-first + Drive-backed sync
- Cross-App regression protection
- data/privacy separation
- functional-app identity preservation.

### ADJUST / OWNERSHIP_TRANSFER
- exact subject Sub-Master outputs include divisible learning units, effort range, mode/method recommendation and uncertainty; exact common skeleton remains pending final validation.
- current GUIDE details are canonicalized here at OS level; detailed subject logic remains in Subject/PRACTICE Sub-Masters.
- exact Ready & Set UI/timer/device behavior remains PROJECT-owned until migrated/validated; omission from this OS file is not deletion.
- exact Core 6 visual-character state remains in `PROJECTS/GUIDE_CORE6_CHARACTER.md`; current image production remains HOLD there.

### HOLD
Retain intentionally without silent adoption:
- exact Subject Sub-Master common skeleton
- separate subject conversation apps
- exact TTS provider/plugin
- parent live-availability UI
- automatic supplemental problem generation
- AI forced correction/relearning
- exact AI model names/prices
- exact OAuth client topology
- exact native lock-screen / Live Activity implementation approach
- third-party Claude/Skill/plugin installation bundle
- exact adaptive workload thresholds/formulas until project-approved evidence and validation exist
- assignment-cycle destructive cleanup / retention timing until immutable-original and history boundaries are approved
- unverified runtime/offline/multi-device convergence and Release PASS.

HOLD exit condition: explicit design/implementation need + fit-for-purpose evidence + authority/impact/security/regression validation.

### REJECT / SUPERSEDED
Reject or preserve as superseded lineage:
- always-on mic
- CHECK audio reused as HELP
- fixed mom/dad/both help MP3 logic
- time-only stuck judgment
- guessed long explanation for unknown stuck
- off-topic auto-distraction
- every WHY immediate deep dive
- question/help = weakness
- Guide retaining full control
- self-report = diagnosis
- external Tool/Skill overriding MASTER
- secret API key in client source
- GitHub as private child-learning-data store
- Capacity = mandatory study amount
- 22:00 as forced actual shutdown
- all free time should be study
- blanket copying of external/SNS/AI-tool claims into MASTER.

### UNVERIFIED SOURCE COVERAGE
Exact byte-for-byte recovery of every historical chat turn from the first conversation is not independently available through the current source interfaces. Therefore exact full-history completeness SHALL NOT be claimed. Recoverable original GUIDE REV_04/REV_05 lineage, Handoffs, current conversation/attachments, project MASTER evidence and latest user corrections were used; inaccessible raw history remains `UNVERIFIED_SOURCE_COVERAGE` and shall trigger future recheck if source becomes available.

## 25. VALIDATION / CHANGE GATE

Any material GUIDE change follows TAKY GRAND MASTER:
`SOURCE RECOVERY → DECISION EXTRACTION → COVERAGE MATRIX → APPLICABLE RULES → OWNERSHIP → DATA FLOW → ERROR / OMISSION / CONFLICT CHECK → SELF-CORRECTION → SELF-VALIDATION → INDEPENDENT CROSS-VALIDATION WHEN MATERIAL → IMPACT VALIDATION → REGRESSION VALIDATION → REVERSE TRACE → APPROVAL → CANONICAL WRITE → POST-WRITE VERIFY → HISTORY`

NO USER-AS-QA.
LOGIC PASS ≠ IMPLEMENTATION PASS ≠ RUNTIME PASS ≠ INTEGRATION PASS ≠ RELEASE PASS.

## 26. FINAL PRINCIPLE

`길잡이가 계획 방법을 보여줌 → 아이가 선택함 → 아이가 시작함 → 아이가 시간과 과제를 확인함 → 필요할 때 도움을 요청함 → 결과를 돌아봄 → 다음 계획을 스스로 더 잘 세움`

`길잡이가 WHY를 보여줌 → 아이가 WHY를 알아차림 → 아이가 스스로 WHY를 만듦 → 스스로 탐색함 → 기존 지식과 연결함 → 자기 말로 이해함 → 다음 행동을 스스로 판단함`

Guide's final success = the child needs Guide less over time.
