# LATEST DELTA — DURABLE LEDGER CORE IMPLEMENTATION (PR #146, 2026-09-26)

Previous audit correctly found NO existing executable source. This PR adds a new Node-only owner-neutral implementation:
- `BADGE/badge-award-ledger-store.js`: verified Decision + active badge gates, signed child/badge-scoped append-only logical history (atomic snapshot replacement), exclusive per-scope lock, HMAC chain and verified COMPLETE history source.
- `BADGE/badge-ledger-visual-binding.js`: maps verified history only to LOCKED/EARNED visual render models; no self-award.
- Integration tests use temporary files and explicit TEST-ONLY fake Decision authority, test-only active badge and test-only approved visual record. No real user awards or assets are activated.

**PRODUCTION NOT WIRED**: the actual authoritative Achievement Decision verifier, family/member authentication, active final catalog source, persistent server-side HMAC key management, multi-device durable backend, real approved visual registry snapshot and app consumer are still unresolved. Do NOT put this Node signing key in browser/PWA JS or treat a caller boolean as approval. The test mock is not authentication. HMAC chaining detects edits but alone does not prevent whole-ledger rollback; external checkpoint anchoring/backup policy is still required for production.

The new code is real executable storage/testing, not evidence that previously closed badge logic already had a functioning runtime. Historical 60 catalog remains inactive. No Netlify deployment.

---

# BADGE VERIFIED AWARD LEDGER BRIDGE — SOURCE AUDIT — 2026-09-26

## Scope
Integrate the already-reviewed Badge Award Ledger with the distinct-reacquisition star projection WITHOUT rebuilding or granting awards, inferring from telemetry, activating historical drafts, or changing any app deployment.

## Observed exact main / historical source
- TAKY main at start: `d15dacd95801cf7ef62addda83487a806c97828e`.
- Main repository has the pure `BADGE/badge-reaward-progress.js` projection, central visual registry and renderer, but no executable Award Ledger persistence/read API or Badge award receipt provider.
- Snap-Pop main contains NO badge execution files. A diverged older implementation branch `taky/snap-pop-implementation-2026-09-20` contains `badge-controller.js`: it stores `badgeBehaviorObservations`, `badgeCandidateReviews`, and `badgeEvents`; its `badgeProgress` was generated by event matching/count, not approved Award Ledger receipts. That count route must NOT be reused as reaward authority.
- Hide main exposes child-authored `SELF_REFLECTION_EVIDENCE` only; it forbids direct awards.
- Historical data/source recovery classifies the 60-name catalog as `WORKING_DRAFT_NOT_ACTIVE`.

## Implemented seam (NOT a live connection)
`BADGE/badge-ledger-projection-bridge.js` is a fail-closed, pure consumer that requires a trusted runtime-injected source with:
- `contract: TAKY_TRUSTED_BADGE_AWARD_HISTORY_SOURCE_V1`
- `loadCompleteHistory({child_id,badge_id})`, returning a complete scoped snapshot, checkpoint and row count
- `verifyAwardRow(row,{child_id,badge_id,checkpoint})`, verifying each actual ledger row and normalizing its receipt

It rejects absent connectors, partial history, mismatched member/badge, failed verification, duplicated IDs, out-of-order sequences and any row incompatible with the existing initial-award/reaward projection. Empty verified history = LOCKED; a verified first acquisition = EARNED/0 stars; five further verified reawards = tier promotion per current provisional within-tier rollover.

### Critical trust boundary
A JavaScript object saying `authority:'AWARD_LEDGER'` is NOT proof. The real Award Ledger owner must provide the read and row verification methods via trusted runtime composition, enforce authorization/member scope server-side or via its existing trusted storage, and expose authoritative receipt IDs/order. The local mock in tests is only a contract test, not live production evidence. Do not point this adapter at a badge event counter, candidate list, telemetry log, a user-supplied object, or a historical working catalog.

## OPEN / block reason
`AWARD_LEDGER_OWNER_READ_API_OR_EXECUTABLE_ASSET_NOT_LOCATED`
The exact real persistence owner, retrieval/verification schema and selected app display owner are not yet identified. Until matched: `LIVE_LEDGER_BINDING=BLOCKED`, even if bridge tests pass. No actual live award was created or displayed.

Final approved pastel badge image is also not recovered; both reference sheets stay NOT_APPROVED, and no visual assets have been bound or released.
